from django.db import models

# Create your models here.
# Note: l'app "api" ne porte pas de modèles propres — elle expose la couche
# REST (serializers/views/urls) pour les modèles définis dans alertes/capteurs/users.

Django==5.0.8
djangorestframework==3.15.2
psycopg2-binary==2.9.9
channels==4.1.0
daphne==4.1.2
channels-redis==4.2.0
redis==5.0.8
celery==5.3.6
django-celery-beat==2.6.0
django-environ==0.11.2
djangorestframework-simplejwt
djangorestframework-gis==0.18
gunicorn==21.2.0
pytest==8.0.0
pytest-django==4.8.0
pytest-cov==7.1.0
paho-mqtt==2.1.0
Pillow==12.3.0
firebase-admin==6.5.0
django-cors-headers==4.4.0
requests==2.32.3
twilio==9.3.2
drf-spectacular==0.27.1

# Service de prédiction IA (mbeund_mi_ia/ia/service_prediction.py), importé
# directement par alertes/tasks.py:appel_modele_ia. Seul RandomForest est
# utilisé dans la classification de risque actuelle (LSTM/TensorFlow non
# appelé) — TensorFlow reste donc optionnel pour Django ; s'il est installé,
# protobuf>=6.31.1 est requis (TensorFlow récent est généré avec ce gencode,
# une version de protobuf plus ancienne plante l'import avec une VersionError).
numpy==2.2.6
pandas==2.3.3
scikit-learn==1.7.2
protobuf==6.31.1

# Détection satellite d'inondation (mbeund_mi_ia/gee/detection_inondation.py),
# importée par alertes/tasks.py:analyse_gee_periodique. Nécessite en plus une
# authentification manuelle une fois par machine (`earthengine authenticate
# --project=<GEE_PROJECT_ID>`) et GEE_PROJECT_ID dans .env — voir
# mbeund_mi_ia/gee/config_gee.py.
earthengine-api==1.7.29
